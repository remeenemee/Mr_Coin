[tool.poetry]
name = "mr-coin"
version = "0.1.0"
description = ""
authors = ["Rem."]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
